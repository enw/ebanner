{
  "name": "browser-test-for-ebanner",
  "version": "0.0.0",
  "description": "test ebanner in a browser",
  "main": "browser.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify browser.js -o bundle.js",
    "watch": "watchify browser.js -o bundle.js --debug --verbose"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ebanner": "^0.2.3"
  }
}
