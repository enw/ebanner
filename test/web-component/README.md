# e-banner web component 

## Overview
(slightly-modified) e-banner wrapped in a polymer web component

## Installation
1. execute ```bower install``` to build client-side dependencies
2. view ```index.html``` in a browser
